# Prompt Optimisation Report

> This file is auto-generated by `src/observability/report.py` after each
> optimisation run. Run the system to populate it with actual results.

## Status

**Not yet generated.** Run the optimisation pipeline first:

```bash
python -m src.main --config config/default.yaml
```

The report will include:

- **Seed vs Final Prompt** — full text of both, plus a unified diff
- **Test-Set Scores** — aggregate and per-section comparison
- **Score Curve** — iteration-by-iteration scores with accept/reject status
- **Accepted Mutations** — which iterations improved the prompt and why
- **Regression Analysis** — fields where scores dropped between accepted iterations
- **Budget Usage** — total iterations, tokens, cost, and elapsed time
- **Limitations** — known constraints and recommendations
